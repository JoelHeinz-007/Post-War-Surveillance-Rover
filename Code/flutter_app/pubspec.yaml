name: rover_controller
description: A defense-grade rover controller app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  google_fonts: ^6.1.0
  flutter_joystick: ^0.3.1  # Adds a professional joystick UI

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
